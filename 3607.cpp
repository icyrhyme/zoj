#include <cstdio>
#define MAXN 1024
using namespace std;
int main() {
	int cs, a[MAXN], t[MAXN];
	scanf("%d", &cs);
	while(cs--) {
		for(int i = 0; i < n; ++i)
			scanf("%d", &a[i]);
		for(int i = 0; i < n; ++i) {
			scanf("%d", &t[i]);
		}
		for(int i = n - 1; i >= 1; --i)
			t[i] = t[i] - t[i - 1];
		int maxi = -1;
		for(int i = 0; i < n; ++i) {
		}
	}
	return 0;
}
